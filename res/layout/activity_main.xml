<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.firoz.cardiacdemo.MainActivity"
    android:descendantFocusability="beforeDescendants"
    android:focusable="true"
    android:focusableInTouchMode="true" >
   
    <AutoCompleteTextView
        android:id="@+id/textViewAuto"
        android:layout_width="fill_parent"
        android:layout_alignParentLeft="true"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginTop="30dp"
        android:background="@drawable/rounded_edittext"
        android:drawableRight="@drawable/search_iconb"
        android:drawableLeft="@drawable/edittextmargin"
        android:drawablePadding="15dp"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:hint="@string/tvAuto"
        android:imeOptions="actionDone"
        android:layout_height="50dp" />

    <ScrollView
        android:id="@+id/scrollView1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/textViewAuto"
        android:keepScreenOn="true"
        android:scrollbarStyle="insideInset"
        android:scrollbars="vertical"
        android:visibility="visible" >

        <TextView
            android:id="@+id/textViewOutput"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="true"
            android:layout_below="@+id/textViewAuto"
            android:layout_marginLeft="25dp"
            android:layout_marginRight="20dp"
            android:drawableTop="@drawable/backtest"
            android:layout_marginTop="30dp"
            android:fontFamily="verdana"
            android:scrollbars="vertical"
            android:textSize="16sp" />
    </ScrollView>

</RelativeLayout>